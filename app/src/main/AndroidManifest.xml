<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alexs.myassignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.ActivityRevise"
            android:label="@string/title_activity_revise"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UI.ActivityAddWord"
            android:label="@string/title_activity_add_word"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".dataBase.ActivityNotes"
            android:label="@string/title_activity_notes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UI.ActivityAskLang" />
        <activity android:name=".UI.ActivityAbout"></activity>
    </application>

</manifest>